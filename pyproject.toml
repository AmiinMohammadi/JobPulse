[project]
name = "jobpulse"
version = "0.1.0"
description = "Analyze your resume against real-time job market demand using LLMs. Extract required skills from job postings, detect skill gaps, and get career recommendations."
readme = "README.md"
authors = [
    { name = "M.A.Mohammaadi", email = "amin.mohammadii.dev@gmail.com" }
]
requires-python = ">=3.11"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "scrapy>=2.19.0",
]

[tool.uv]
package = false
