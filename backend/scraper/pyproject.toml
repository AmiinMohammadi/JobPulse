[project]
name = "jobpulse-scraper"
version = "0.1.0"
description = "Job Pulse Stage 1: Scrapy spiders that collect raw JobVision job postings (sitemap discovery + detail extraction). Raw HTML/text only — cleaning happens in Stage 2."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "scrapy>=2.11.0",
]

[tool.uv]
package = false
