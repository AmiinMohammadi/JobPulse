# مستندات استیج ۶ — Generation (تحلیل شکاف مهارت با Gemini LLM)
> **مخاطب:** همکار گرامی پروژه JobPulse  
> **هدف سند:** توضیح دقیق کارهای انجام شده در استیج ۶، معماری و نحوه استفاده از آن برای استیج‌های بعدی (به‌خصوص استیج ۷: Backend API و استیج ۸: Frontend)، به همراه نکات حساس و موارد مشکوک.

---

## ۱. خلاصه و هدف استیج ۶ (Overview & Goal)

در استیج ۶، لایه **Generation** با استفاده از هوش مصنوعی گوگل (Google Gemini API) پیاده‌سازی شد.  
هدف این لایه این است که:
1. متن رزومه کاندیدا (`resume_text`)
2. لیستی از آگهی‌های شغلی استخراج‌شده از دیتابیس برداری (`matched_jobs` که از استیج ۵ می‌آید)

را دریافت کرده و یک تحلیل شکاف مهارتی (Skill-Gap Analysis) ساختاریافته و تضمین‌شده به صورت JSON تولید کند.

---

## ۲. فایل‌های ایجاد شده و تغییر یافته (Files Created & Modified)

| مسیر فایل | وضعیت | توضیحات تغییرات |
| :--- | :--- | :--- |
| `backend/app/retrieval/generation.py` | **جدید (New)** | هسته اصلی استیج ۶ شامل پرامپت، تابع فراخوانی جمینای، هندلینگ مدل پشتیبان (Cascade)، کنترل نرخ (Backoff) و بازگردانی JSON ساختاریافته. |
| `backend/app/retrieval/__init__.py` | **جدید (New)** | معرفی ماژول `retrieval` و اکسپورت تابع `generate_gap_analysis`. |
| `backend/tests/test_generation.py` | **جدید (New)** | تست‌های جامع استیج ۶ (تست زنده با جمینای، تست اعتبارسنجی خروجی، تست نبود کلید API، تست لیست خالی). |
| `backend/app/core/config.py` | **ویرایش (Modified)** | فعال‌سازی فیلد `gemini_api_key: str | None = None` در کلاس تنظیمات Pydantic برای خواندن خودکار از `.env`. |
| `backend/pyproject.toml` | **ویرایش (Modified)** | افزودن کتابخانه رسمی `google-genai>=1.0.0` به وابستگی‌های پروژه. |
| `backend/uv.lock` | **ویرایش (Modified)** | به‌روزرسانی خودکار قفل وابستگی‌ها با استفاده از `uv`. |
| `.gitignore` | **ویرایش (Modified)** | اطمینان از نادیده‌گرفتن فایل‌های `.env` و فایل‌های موقت تست مثل `gap_analysis_output.json`. |

---

## ۳. نحوه طراحی و معماری ماژول (Design & Architecture)

تابع اصلی در مسیر `app.retrieval.generation` قرار دارد:

```python
def generate_gap_analysis(
    resume_text: str,
    matched_jobs: list[dict],
) -> dict[str, Any]:
```

### الف) مشخصات ورودی‌ها (Input Specification)
- **`resume_text: str`**: متن خام رزومه (استخراج‌شده از PDF/DOCX یا متن ورودی کاربر).
- **`matched_jobs: list[dict]`**: لیستی از دیکشنری‌های آگهی‌های شغلی. هر دیکشنری باید شامل این فیلدها باشد:
  - `id`: شناسه یکتای آگهی
  - `title`: عنوان شغلی (مثلاً "برنامه‌نویس پایتون")
  - `company`: نام شرکت
  - `similarity_score`: امتیاز شباهت به دست آمده از بردارها
  - `source_url`: لینک آگهی
  - `description_clean`: متن پاک‌سازی شده آگهی (حاوی شرح وظایف و مهارت‌های مورد نیاز)

### ب) مشخصات خروجی تضمین‌شده (Output Contract)
خروجی این تابع **همیشه** یک دیکشنری پایتون با سه کلید قطعی است:
```json
{
  "matching_skills": [
    "Python",
    "FastAPI",
    "PostgreSQL",
    "Docker"
  ],
  "missing_skills": [
    "Django",
    "RabbitMQ",
    "Redis",
    "Celery",
    "CI/CD"
  ],
  "summary": "کاندیدا از نظر مهارت‌های پایه‌ای بک‌اند مانند Python و FastAPI با موقعیت‌های شغلی همخوانی خوبی دارد. با این حال، برای تطابق کامل با آگهی‌های ارشد، نیاز به تقویت مهارت در فریم‌ورک Django و ابزارهای صف‌بندی مانند RabbitMQ و Celery دارد."
}
```

### ج) تصمیمات فنی کلیدی (Key Technical Decisions)
1. **استفاده از SDK رسمی جدید (`google-genai`):** به‌جای کتابخانه قدیمی `google-generativeai` که منسوخ شده، از پکیج مدرن `google-genai` استفاده شده است.
2. **استراتژی مدل دوگانه (Model Cascade):**
   - اول مدل `gemini-3.6-flash` (یا مدل سریع استاندارد فلش) فراخوانی می‌شود.
   - در صورت برخورد با خطای محدودیت سهمیه (HTTP 429)، پس از اتمام تلاش‌ها به صورت خودکار روی مدل دوم `gemini-3.5-flash-lite` سوییچ می‌کند.
3. **کنترل هوشمند نرخ خطا (Exponential Backoff):**
   - برای خطای 429، تا ۳ مرتبه تلاش با تاخیر فزاینده (۲ ثانیه، ۴ ثانیه، ...) انجام می‌شود.
4. **خروجی ساختاریافته معتبر (Structured JSON Output):**
   - با استفاده از پارامتر `response_format` و تعریف `JSON Schema` صریح، مدل ملزم به برگرداندن ساختار دقیق JSON بدون متن اضافه است. همچنین یک پاک‌کننده خودکار برای تگ‌های Markdown (` ```json `) تعبیه شده است.
5. **جلوگیری از توهم مدل (Anti-Hallucination Rules):**
   - پرامپت سیستم صریحاً دستور داده که تنها مهارت‌هایی که عیناً در متن آگهی‌ها ذکر شده‌اند باید استخراج شوند.
6. **پایداری بدون کرش (Graceful Degradation):**
   - اگر کلید API ست نشده باشد، یا خطای غیرمنتظره اینترنت رخ دهد، به جای `crash` کردن سرور، یک دیکشنری امن با آرایه‌های خالی و خلاصه خالی برگردانده می‌شود:
     ```python
     {"matching_skills": [], "missing_skills": [], "summary": ""}
     ```

---

## ۴. راهنمای استفاده برای همکار (How to Use in Stage 7 & 8)

اگر در حال پیاده‌سازی **Stage 7 (FastAPI Endpoint)** یا **Stage 8 (Frontend)** هستید، این‌گونه از این استیج استفاده کنید:

### نمونه کد فراخوانی در اندپوینت FastAPI:
```python
from app.retrieval.generation import generate_gap_analysis

# فرض کنید از استیج ۵ آگهی‌ها را گرفتید:
# matched_jobs = retrieve_relevant_jobs(resume_text, top_k=5)

analysis_result = generate_gap_analysis(
    resume_text=resume_text,
    matched_jobs=matched_jobs,
)

# دسترسی به خروجی:
matching = analysis_result["matching_skills"]  # list[str]
missing = analysis_result["missing_skills"]    # list[str]
summary = analysis_result["summary"]          # str (متن فارسی)
```

### اجرای تست‌های استیج ۶:
برای اطمینان از سلامت این بخش، کافیست در پوشه `backend` دستور زیر را اجرا کنید:
```bash
uv run pytest tests/test_generation.py -v
```

---

## ۵. موارد مشکوک، ریسک‌ها و نکاتی که باید حواسمون باشه (Watch-outs & Gotchas)

> [!WARNING]
> این موارد را حتماً در زمان ادغام استیج‌های ۷، ۸ و دیپلوی مد نظر داشته باشید:

1. **محدودیت سهمیه پلن رایگان جمینای (Rate Limits / Quota):**
   - پلن رایگان Gemini دارای محدودیت تعداد درخواست در دقیقه (RPM معمولاً ۱۵) و تعداد درخواست در روز (RPD) است.
   - اگر در تست‌ها یا در فرانت‌اند درخواست‌های متوالی و سریع زده شود، خطای 429 رخ می‌دهد. مکانیزم Retry در کد وجود دارد اما کاربر ممکن است چند ثانیه معطل شود.

2. **طول پرامپت و تعداد آگهی‌ها (Latency & Context Length):**
   - ارسال متن کامل ۱۰ یا ۱۵ آگهی شغلی طولانی می‌تواند حجم توکن را بالا ببرد و زمان پاسخ‌دهی (Latency) را به **۱۰ تا ۲۰ ثانیه** برساند.
   - **پیشنهاد برای استیج ۷:** در اندپوینت `POST /analyze` توصیه می‌شود حداکثر **۳ تا ۵ آگهی برتر** (`top_k=3` تا `5`) که بیشترین `similarity_score` را دارند به عنوان متن ورودی به Gemini فرستاده شوند.
   - **پیشنهاد برای استیج ۸:** در رابط کاربری فرانت‌اند، حتماً لودینگ یا Progress Bar مناسب پیاده‌سازی شود تا کاربر در طول انتظار خسته نشود.

3. **تنظیم کلید GEMINI_API_KEY در محیط‌های مختلف:**
   - متغیر محیطی `GEMINI_API_KEY` باید در فایل `backend/.env` تنظیم شود.
   - در صورت نبود این کلید، کد خطا (Exception) پرتاب نمی‌کند، بلکه نتیجه خالی برمی‌گرداند؛ بنابراین اگر دیدید در خروجی هیچ مهارتی نمی‌آید، اولین جایی که باید چک کنید فایل `.env` است.

4. **تفاوت انکودینگ در کنسول ویندوز (Windows cp1252 vs UTF-8):**
   - اگر اسکریپت یا تستی نوشتید که مستقیماً خروجی `summary` فارسی را در کنسول ویندوز پرینت می‌کند، ممکن است با `UnicodeEncodeError: 'charmap' codec can't encode...` روبرو شوید. دلیل این است که کنسول ویندوز معمولاً روی `cp1252` است. خروجی در قالب JSON یا در بستر وب کاملاً UTF-8 و سالم است و مشکلی ندارد.

5. **نام‌گذاری مدل‌ها در آینده (Model Deprecation):**
   - در حال حاضر مدل‌های سری `gemini-2.5` یا `gemini-3.6` در حال توسعه هستند. اگر در آینده گوگل نسخه‌ای را از رده خارج کرد، کافیست متغیرهای `_PRIMARY_MODEL` و `_FALLBACK_MODEL` در بالای فایل `backend/app/retrieval/generation.py` به‌روزرسانی شوند.
